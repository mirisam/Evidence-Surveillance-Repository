---
title: "References"
format:
  html:
    page-layout: full
---


## Evidence database

This page displays a searchable and filterable evidence table generated from `data/references.csv`.

Use the filters below to narrow references by outbreak, study design, or year. The complete dataset can also be downloaded as a CSV.

```{ojs}
//| echo: false

references = FileAttachment("data/references.csv").csv({typed: false})
```

```{ojs}
//| echo: false

uniqueValues = (data, column) => [
  "All",
  ...Array.from(new Set(data.map(d => d[column]).filter(d => d !== ""))).sort()
]
```

## Filter the evidence

```{ojs}
//| echo: false

viewof outbreak_filter = Inputs.select(
  uniqueValues(references, "Outbreak"),
  {label: "Outbreak"}
)
```

```{ojs}
//| echo: false

viewof study_design_filter = Inputs.select(
  uniqueValues(references, "Study_Design"),
  {label: "Study design"}
)
```

```{ojs}
//| echo: false

viewof year_filter = Inputs.select(
  uniqueValues(references, "Year"),
  {label: "Year"}
)
```

```{ojs}
//| echo: false

filtered_references = references.filter(d =>
  (outbreak_filter === "All" || d.Outbreak === outbreak_filter) &&
  (study_design_filter === "All" || d.Study_Design === study_design_filter) &&
  (year_filter === "All" || d.Year === year_filter)
)
```

## Browse references

```{ojs}
//| echo: false

Inputs.table(filtered_references, {
  columns: ["Title", "Authors", "Year", "Outbreak", "Study Design", "Journal"],
  sort: "Year",
  reverse: true
})
```

<a href="data/references.csv" download="references.csv" class="btn btn-primary">
Download CSV
</a>

::: {.status-box}
**Note:** This table uses sample data for demonstration purposes. In a future workflow, the CSV would be replaced with an export from DistillerSR.
:::